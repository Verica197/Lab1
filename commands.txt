mkdir lab1
cd lab1
git init
notepad l1.txt
git add l1.txt
git commit -m "Initial commit"
echo "PET" >> l1.txt
git add l1.txt
git commit -m "modify l1.txt"
git remote add origin https://github.com/Verica197/Lab1.git
git push -u origin master
git reset --hard
1git chekout -b logging
git log >> log.txt
git add log.txt
git commit -m "Added log file"
git push -u origin logging
git merge master
git checkout master
git merge logging
git commit -m "Merged branches master and logging"
git push
git checkout -b allcommands
notepad commands.txt